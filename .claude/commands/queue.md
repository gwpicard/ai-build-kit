---
description: The whole list of ready work at once, for somebody taking on more than one piece.
disable-model-invocation: true
---
<!-- GENERATED from .agents/skills/queue/. Do not edit here; regenerate with .agents/tools/build-adapters.sh -->

When this command comes from a Claude plugin, load and follow `${CLAUDE_PLUGIN_ROOT}/.agents/skills/queue/SKILL.md`. Otherwise, load and follow `.agents/skills/queue/SKILL.md`. It is the single source of truth for the `/queue` command. Treat anything typed after the command as the user's request and pass it through unchanged.
