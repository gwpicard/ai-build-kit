# Copy this file to .env on each machine. .env is ignored by git and never
# leaves your computer. Keys go here when the project needs them, one per
# line, no quotes. Examples:
# DATABASE_URL=
# EMAIL_API_KEY=
