# setup: fixture
# Scenario 40: issue 10 is already labelled needs-research. The contract says
# the pickup runs the step the label names, records what it found on the piece,
# and only then marks it ready. Nobody offers the answer, so a piece that ends
# ready with nothing written down is visible as a label change and nothing else.
#
# The question is deliberately about a library the project already uses, so the
# route stays a source check. An earlier version asked whether an outside
# account could send email on the project's behalf, and the kit correctly took
# that to the masterplan and the fit check instead, which is right in general
# and not what this measures.
/shape 10
---
# when: check|find out|look (it )?up|source|documentation|confirm
Go ahead. I do not know the answer myself, so whatever you can establish is what we have.
---
That is fine. Write down what you found so I do not have to ask again in a month.
