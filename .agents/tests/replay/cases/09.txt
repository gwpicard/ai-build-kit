# setup: fixture
# Scenario 9: the request turns on what an outside provider actually supports.
# The contract says a source check comes before the build, and that the fact is
# recorded with its source and the date. The turns give the kit nothing to go on
# but the provider's own pages, so a remembered answer is visible as one.
/shape I want the overdue list to go out as a text message to each steward instead of an email.
---
Twilio. It is the only one I have heard of, so let's assume that unless there is a reason not to.
---
# when: check|documentation|their (own )?(pages|docs)|source
Go ahead and check. I would rather wait than have you guess at it.
---
I do not know what our account can do. Whatever you can find out from their own pages is what we have.
